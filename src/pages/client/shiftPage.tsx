import Layout from '@/layout/layout'
import Shift from '@/components/client/shift/shift'

const ShiftPage = () => {
  return (
    <Layout>
      <Shift />
    </Layout>
  )
}

export default ShiftPage
